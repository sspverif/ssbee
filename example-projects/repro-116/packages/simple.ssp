package Simple {
    params {
        b: Bool,
    }

    oracle Sample() -> Bits(256){
        if b {
            x <-$ Bits(256) sample-name first;
            return x;
        } else {
            x <-$ Bits(256) sample-name second;
            return x;
        }
    }
}
